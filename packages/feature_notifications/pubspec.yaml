name: feature_notifications
description: >-
  Notifications feature domain layer for flutter-starter-template:
  entities, repository contract, use cases, and sync controller interface.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  architecture: ^0.1.0
  injectable: ^3.0.0

dev_dependencies:
  build_runner: ^2.15.0
  injectable_generator: ^3.0.2
  test: ^1.25.0
